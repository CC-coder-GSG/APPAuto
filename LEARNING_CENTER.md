# 学习中心与答题 App 使用说明

## 已实现流程

1. 任意已登录成员可在网页“学习中心”创建学习资料、课程和答题活动；安卓端只提供学习、答题与查成绩，不提供出题。
   - 每项资料或课程可以上传多个教学附件，单个文件最大 100 MB。
   - PPT/PPTX 上传后由服务端转换为 PDF 预览；PDF 直接预览；Markdown 以安全文本方式预览；原文件均可下载。
2. 新活动是草稿，除出题人外，其他账号看不到活动和题目。出题人可反复进行预览答题、查看自动判分并人工批阅预览答卷。
3. 出题人点击“开始答题”后，团队成员才能看到题目并提交。
4. 同一个账号在网页和安卓 App 合计只能提交一份正式答卷；服务端校验和数据库唯一约束都会阻止重复提交。
5. 出题人结束答题后，在“批阅与统计”中查看每道题正确率、全部成员答案，并给简答题评分。
6. 所有正式答卷批阅完成后，可选择“发布成绩并展示答案”或“发布成绩但隐藏答案”。答题人随后可以在网页或 App 查看成绩。

## 题型与判分方式

- 单选题：答案与唯一正确选项一致时自动得分。
- 多选题：所选选项集合与正确选项集合完全一致时自动得分。
- 填空题：支持以下五种策略。
  - 必须一字不差：填写文本与任一参考答案完全一致。
  - 连续包含答案：填写文本连续包含任一参考答案，不区分英文大小写。
  - 规范化匹配：去除空白、标点和符号并统一全半角、英文大小写后，与任一答案一致。
  - 匹配任一答案：填写内容可用换行、逗号或分号分隔，规范化后命中任一给定答案即可。
  - 匹配指定数量：分隔后的填写项至少命中出题人指定数量的参考答案。
- 简答题：由出题人在网页端人工评分，可写批语。

## 部署升级

拉取代码、安装原有依赖并重启 FastAPI 服务即可。启动时 SQLAlchemy 会自动创建学习中心和多端登录所需的新表，不会删除现有数据。

PPT/PPTX 在线预览依赖服务端 LibreOffice。默认调用 `soffice`；如可执行文件不在 PATH 中，可通过 `APP_LIBREOFFICE_BIN` 指定路径。转换不可用时上传和下载仍正常，界面会隐藏该文件的预览按钮。

`auto_main` 已支持网页端与移动端各自维护会话。移动页面登录时会声明为移动端，因此网页与 App 可以同时在线；同一终端类型再次登录仍会使该终端的旧登录失效。修改或重置密码仍会使相关旧会话失效。

## 安卓端

最新可安装测试包：`release/OmniQA-Learning-Android-1.0.2-debug.apk`

首次启动时填写服务端地址，例如：

```text
http://192.168.1.20:8000
```

手机必须能访问该地址。局域网试用允许 HTTP；正式对外使用应配置 HTTPS。App 顶部“服务器”按钮可以修改地址。

源码位于 `android/`，详见其中的 `README.md`。当前 APK 使用 Android 调试证书签名，适合组内验收；正式分发前需要使用团队自己的长期 keystore 生成 release 包。

## 当前发布范围

答题活动默认发布给所有 `is_team_member=true` 的团队成员，统计中的“应答人数”也按此口径计算，并排除出题人。若后续需要按部门、项目或指定名单发布，可在现有活动模型上继续增加参与范围。
